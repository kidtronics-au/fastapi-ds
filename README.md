FastAPI chst endpoints using OpenAI API / LangGraph 

uvicorn main:app --reload

psql -h localhost -U postgres -d mydb

docker volume create --driver local \
  --opt type=none \
  --opt device=/home/guru/Projects/ollama/.models \
  --opt o=bind \
  my_ollama_volume
my_ollama_volume

docker exec ollama-rocm ollama pull mistral:7b